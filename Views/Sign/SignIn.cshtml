@{
    ViewData["Title"] = "Porsche - SignIn";
    Layout = "_Layout";
}

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/css/SignIn.css" asp-append-version="true" />
</head>
<body>
    <div class="Container">
        <!-- Image -->
        <div class="CContainer">
            <img src="~/images/Sign/Logo.png" alt="Logo" class="logoimg">
        </div>
        <div class="Containerlogin">
            <!-- email -->
            <div class="field">
                <input id="Email" placeholder="Email">
                @* @Html.TextBoxFor(m => m.Description, new { @placeholder = "Description", @required = "required", @maxlength = "200" })  *@
                <div class="line"></div>
            </div>
        </div>
        <div class="Containerlogin">
            <!-- password -->
            <div class="field">
                <input type="Password" id="PI" placeholder="Password">
                @* @Html.TextBoxFor(m => m.Event_Id, new { @type = "number", @placeholder = "EventID  ", @required = "required", @maxlength = "10" }) *@
                <div class="line"></div>
                <img src="~/images/Sign/Hide-Pass.png" class="hideicon" id="H">
            </div>
        </div>
        <!-- SignIn -->
        <div class="TContainer">
            <p style="color: snow;margin:0;font-size:14px">
                Don't Have An Account?
            </p>
        </div>
        <div class="BContainer" style="margin-top:5px;">
            <a href="~/Sign/SignUp">
                <button type="button" class="button-style">SignUp</button>
            </a>
        </div>
        <!-- Submit -->
        <div class="BContainer" style="margin-top: 50px;">
            <button class="submitionbutton" role="button">Login</button>
        </div>
    </div>
    <script type="text/javascript" src="~/js/SignIn.js"></script>
</body>